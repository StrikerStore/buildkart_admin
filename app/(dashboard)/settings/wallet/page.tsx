import { redirect } from 'next/navigation';

/** Moved under Discounts. Kept so an old bookmark still lands somewhere. */
export default function WalletSettingsMoved() {
  redirect('/wallet-cashback');
}
